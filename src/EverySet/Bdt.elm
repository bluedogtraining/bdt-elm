module EverySet.Bdt exposing (toggle)

{-| EverySet Helpers

# Toggle
@docs toggle

-}

--import EverySet exposing (EverySet)
--
--
--{-| Toggle an item in a set. Item is added if not existing, or removed if existing.
---}
--toggle : a -> EverySet a -> EverySet a
--toggle item set =
--
--    if EverySet.member item set
--    then EverySet.remove item set
--    else EverySet.insert item set


{-|
-}
toggle : String
toggle =
    "hi"